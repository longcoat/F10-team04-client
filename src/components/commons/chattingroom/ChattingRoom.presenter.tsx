import * as CH from './ChattingRoom.styles'
import { useContext } from 'react'
import { GlobalContext } from '../../../../pages/_app'
export default function ChattingRoomUI(props) {
  const { accessToken } = useContext(GlobalContext)
  return <></>
}
